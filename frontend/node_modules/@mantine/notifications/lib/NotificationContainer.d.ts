import { NotificationProps } from '@mantine/core';
import { NotificationData } from './notifications.store';
interface NotificationContainerProps extends NotificationProps {
    data: NotificationData;
    onHide: (id: string) => void;
    autoClose: number | false;
    transitionDuration: number;
    allowDragDismiss: boolean;
    allowScrollDismiss: boolean;
    paused: boolean;
    onHoverStart?: () => void;
    onHoverEnd?: () => void;
    onExpandRequest?: () => void;
    ref?: React.Ref<HTMLDivElement>;
    renderNotification?: ((notification: NotificationData) => React.ReactNode) | null;
    layout?: 'default' | 'stacked';
    stackIndex?: number;
    stackSize?: number;
    stackPosition?: string;
    stackExpanded?: boolean;
    stackExpandedOffset?: number;
    transitionState?: string;
}
export declare function NotificationContainer({ data, onHide, autoClose, transitionDuration, allowDragDismiss, allowScrollDismiss, paused, onHoverStart, onHoverEnd, onExpandRequest, ref, style, renderNotification, layout, stackIndex, stackSize, stackPosition, stackExpanded, stackExpandedOffset, transitionState, ...others }: NotificationContainerProps): import("react").JSX.Element;
export declare namespace NotificationContainer {
    var displayName: string;
}
export {};
