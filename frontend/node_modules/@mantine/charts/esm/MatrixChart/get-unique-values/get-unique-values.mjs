"use client";
//#region packages/@mantine/charts/src/MatrixChart/get-unique-values/get-unique-values.ts
function getUniqueValues(data, axis, labels) {
	if (labels) return labels;
	const seen = /* @__PURE__ */ new Set();
	const result = [];
	data.forEach((cell) => {
		const value = cell[axis];
		if (!seen.has(value)) {
			seen.add(value);
			result.push(value);
		}
	});
	return result;
}
//#endregion
export { getUniqueValues };

//# sourceMappingURL=get-unique-values.mjs.map