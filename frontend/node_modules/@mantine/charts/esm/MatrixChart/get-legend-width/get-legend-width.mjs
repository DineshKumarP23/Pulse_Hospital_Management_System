"use client";
//#region packages/@mantine/charts/src/MatrixChart/get-legend-width/get-legend-width.ts
function getLegendWidth({ legendLabels, fontSize, colorsCount, cellSize, gap }) {
	const textGap = 6;
	const charWidth = fontSize * .6;
	const lessWidth = legendLabels[0].length * charWidth;
	const allColorsCount = 1 + colorsCount;
	const rectsWidth = allColorsCount * cellSize + (allColorsCount - 1) * gap;
	const moreWidth = legendLabels[1].length * charWidth;
	return lessWidth + textGap + rectsWidth + textGap + moreWidth;
}
//#endregion
export { getLegendWidth };

//# sourceMappingURL=get-legend-width.mjs.map