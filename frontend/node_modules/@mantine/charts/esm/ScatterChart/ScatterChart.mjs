"use client";
import { ChartTooltip } from "../ChartTooltip/ChartTooltip.mjs";
import { ChartLegend } from "../ChartLegend/ChartLegend.mjs";
import grid_chart_module_default from "../grid-chart.module.mjs";
import { Box, createVarsResolver, factory, getThemeColor, useMantineTheme, useProps, useResolvedStylesApi, useStyles } from "@mantine/core";
import { jsx, jsxs } from "react/jsx-runtime";
import { CartesianGrid, Label, LabelList, Legend, ReferenceArea, ReferenceDot, ReferenceLine, ResponsiveContainer, Scatter, ScatterChart, Tooltip as Tooltip$1, XAxis, YAxis } from "recharts";
import { useState } from "react";
//#region packages/@mantine/charts/src/ScatterChart/ScatterChart.tsx
function getAxis(key, dataKey) {
	return key === dataKey.x ? "x" : "y";
}
const defaultProps = {
	withXAxis: true,
	withYAxis: true,
	withTooltip: true,
	tooltipAnimationDuration: 0,
	tickLine: "y",
	strokeDasharray: "5 5",
	gridAxis: "x",
	accessibilityLayer: true
};
const varsResolver = createVarsResolver((theme, { textColor, gridColor }) => ({ root: {
	"--chart-text-color": textColor ? getThemeColor(textColor, theme) : void 0,
	"--chart-grid-color": gridColor ? getThemeColor(gridColor, theme) : void 0
} }));
const ScatterChart$1 = factory((_props) => {
	const props = useProps("ScatterChart", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, referenceLines, referenceAreas, referenceDots, dir, withLegend, withTooltip, withXAxis, withYAxis, withRightYAxis, xAxisProps, yAxisProps, rightYAxisProps, orientation, scatterChartProps, legendProps, data, gridAxis, tickLine, strokeDasharray, gridProps, tooltipAnimationDuration, tooltipProps, children, onMouseLeave, dataKey, textColor, gridColor, xAxisLabel, yAxisLabel, rightYAxisLabel, unit, labels, valueFormatter, scatterProps, pointLabels, attributes, accessibilityLayer, ...others } = props;
	const getFormatter = (axis) => typeof valueFormatter === "function" ? valueFormatter : valueFormatter?.[axis];
	const xFormatter = getFormatter("x");
	const yFormatter = getFormatter("y");
	const theme = useMantineTheme();
	const mappedData = data.map((item) => ({
		...item,
		data: item.data.map((point) => ({
			...point,
			name: item.name
		}))
	}));
	const { resolvedClassNames, resolvedStyles } = useResolvedStylesApi({
		classNames,
		styles,
		props
	});
	const getStyles = useStyles({
		name: "ScatterChart",
		classes: grid_chart_module_default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const withXTickLine = gridAxis !== "none" && (tickLine === "x" || tickLine === "xy");
	const withYTickLine = gridAxis !== "none" && (tickLine === "y" || tickLine === "xy");
	const [highlightedArea, setHighlightedArea] = useState(null);
	const shouldHighlight = highlightedArea !== null;
	const handleMouseLeave = (event) => {
		setHighlightedArea(null);
		onMouseLeave?.(event);
	};
	const referenceLinesItems = referenceLines?.map(({ color: lineColor, labelPosition, ...line }, index) => {
		const color = getThemeColor(lineColor, theme);
		return /* @__PURE__ */ jsx(ReferenceLine, {
			stroke: lineColor ? color : "var(--chart-grid-color)",
			strokeWidth: 1,
			...line,
			label: {
				fill: lineColor ? color : "currentColor",
				fontSize: 12,
				position: labelPosition ?? "insideBottomLeft",
				...typeof line.label === "object" ? line.label : { value: line.label }
			},
			...getStyles("referenceLine")
		}, index);
	});
	const referenceAreasItems = referenceAreas?.map(({ color: areaColor, labelPosition, ...area }, index) => {
		const color = getThemeColor(areaColor, theme);
		return /* @__PURE__ */ jsx(ReferenceArea, {
			fill: areaColor ? color : "var(--chart-grid-color)",
			fillOpacity: .2,
			stroke: areaColor ? color : "var(--chart-grid-color)",
			strokeOpacity: .6,
			...area,
			label: {
				fill: areaColor ? color : "currentColor",
				fontSize: 12,
				position: labelPosition ?? "insideTop",
				...typeof area.label === "object" ? area.label : { value: area.label }
			},
			...getStyles("referenceArea")
		}, index);
	});
	const referenceDotsItems = referenceDots?.map(({ color: dotColor, labelPosition, ...dot }, index) => {
		const color = getThemeColor(dotColor, theme);
		return /* @__PURE__ */ jsx(ReferenceDot, {
			r: 5,
			fill: dotColor ? color : "var(--chart-grid-color)",
			stroke: "var(--mantine-color-body)",
			strokeWidth: 2,
			...dot,
			label: {
				fill: dotColor ? color : "currentColor",
				fontSize: 12,
				position: labelPosition ?? "top",
				...typeof dot.label === "object" ? dot.label : { value: dot.label }
			},
			...getStyles("referenceDot")
		}, index);
	});
	const scatters = mappedData.map((item, index) => {
		const dimmed = shouldHighlight && highlightedArea !== item.name;
		return /* @__PURE__ */ jsxs(Scatter, {
			data: item.data,
			fill: getThemeColor(item.color, theme),
			isAnimationActive: false,
			fillOpacity: dimmed ? .1 : 1,
			yAxisId: item.yAxisId || void 0,
			...scatterProps,
			children: [pointLabels && /* @__PURE__ */ jsx(LabelList, {
				dataKey: dataKey[pointLabels],
				fontSize: 8,
				dy: 10
			}), scatterProps?.children]
		}, index);
	});
	const sharedYAxisProps = {
		type: "number",
		axisLine: false,
		dataKey: dataKey.y,
		tickLine: withYTickLine ? { stroke: "currentColor" } : false,
		allowDecimals: true,
		unit: unit?.y,
		tickFormatter: yFormatter,
		...getStyles("axis")
	};
	return /* @__PURE__ */ jsx(Box, {
		...getStyles("root"),
		onMouseLeave: handleMouseLeave,
		dir: dir || "ltr",
		...others,
		children: /* @__PURE__ */ jsx(ResponsiveContainer, {
			...getStyles("container"),
			children: /* @__PURE__ */ jsxs(ScatterChart, {
				margin: {
					bottom: xAxisLabel ? 30 : void 0,
					left: yAxisLabel ? 10 : void 0,
					right: yAxisLabel || rightYAxisLabel ? 5 : void 0
				},
				accessibilityLayer,
				...scatterChartProps,
				children: [
					referenceAreasItems,
					/* @__PURE__ */ jsx(CartesianGrid, {
						strokeDasharray,
						vertical: gridAxis === "y" || gridAxis === "xy",
						horizontal: gridAxis === "x" || gridAxis === "xy",
						...getStyles("grid"),
						...gridProps
					}),
					/* @__PURE__ */ jsxs(XAxis, {
						type: "number",
						hide: !withXAxis,
						dataKey: dataKey.x,
						tick: {
							transform: "translate(0, 10)",
							fontSize: 12,
							fill: "currentColor"
						},
						stroke: "",
						interval: "preserveStartEnd",
						tickLine: withXTickLine ? { stroke: "currentColor" } : false,
						minTickGap: 5,
						unit: unit?.x,
						tickFormatter: xFormatter,
						...getStyles("axis"),
						...xAxisProps,
						children: [xAxisLabel && /* @__PURE__ */ jsx(Label, {
							position: "insideBottom",
							offset: -20,
							fontSize: 12,
							...getStyles("axisLabel"),
							children: xAxisLabel
						}), xAxisProps?.children]
					}),
					/* @__PURE__ */ jsxs(YAxis, {
						hide: !withYAxis,
						tick: {
							transform: "translate(-10, 0)",
							fontSize: 12,
							fill: "currentColor"
						},
						...sharedYAxisProps,
						...yAxisProps,
						children: [yAxisLabel && /* @__PURE__ */ jsx(Label, {
							position: "insideLeft",
							angle: -90,
							textAnchor: "middle",
							fontSize: 12,
							offset: -5,
							...getStyles("axisLabel"),
							children: yAxisLabel
						}), yAxisProps?.children]
					}),
					/* @__PURE__ */ jsxs(YAxis, {
						yAxisId: "right",
						orientation: "right",
						hide: !withRightYAxis,
						tick: {
							transform: "translate(10, 0)",
							fontSize: 12,
							fill: "currentColor"
						},
						...sharedYAxisProps,
						...rightYAxisProps,
						children: [rightYAxisLabel && /* @__PURE__ */ jsx(Label, {
							position: "insideRight",
							angle: 90,
							textAnchor: "middle",
							fontSize: 12,
							offset: -5,
							...getStyles("axisLabel"),
							children: rightYAxisLabel
						}), rightYAxisProps?.children]
					}),
					withTooltip && /* @__PURE__ */ jsx(Tooltip$1, {
						animationDuration: tooltipAnimationDuration,
						isAnimationActive: tooltipAnimationDuration !== 0,
						position: { y: 0 },
						cursor: {
							stroke: "var(--chart-grid-color)",
							strokeWidth: 1,
							strokeDasharray
						},
						content: ({ label, payload, labelFormatter }) => /* @__PURE__ */ jsx(ChartTooltip, {
							type: "scatter",
							label: labelFormatter && payload ? labelFormatter(label, payload) : label,
							payload: labels ? payload?.map((item) => ({
								...item,
								name: labels[getAxis(item.name, dataKey)] || item.name,
								value: getFormatter(getAxis(item.name, dataKey))?.(item.value) ?? item.value
							})) : payload?.map((item) => ({
								...item,
								value: getFormatter(getAxis(item.name, dataKey))?.(item.value) ?? item.value
							})),
							classNames: resolvedClassNames,
							styles: resolvedStyles,
							series: data,
							attributes
						}),
						...tooltipProps
					}),
					withLegend && /* @__PURE__ */ jsx(Legend, {
						verticalAlign: "top",
						content: (payload) => /* @__PURE__ */ jsx(ChartLegend, {
							payload: payload.payload?.map((item, index) => ({
								...item,
								dataKey: data[index].name
							})),
							onHighlight: setHighlightedArea,
							legendPosition: legendProps?.verticalAlign || "top",
							classNames: resolvedClassNames,
							styles: resolvedStyles,
							series: data,
							attributes
						}),
						height: 44,
						...legendProps
					}),
					referenceLinesItems,
					scatters,
					referenceDotsItems
				]
			})
		})
	});
});
ScatterChart$1.displayName = "@mantine/charts/ScatterChart";
ScatterChart$1.classes = grid_chart_module_default;
ScatterChart$1.varsResolver = varsResolver;
//#endregion
export { ScatterChart$1 as ScatterChart };

//# sourceMappingURL=ScatterChart.mjs.map