"use client";
import { ChartLegend } from "../ChartLegend/ChartLegend.mjs";
import RadialBarChart_module_default from "./RadialBarChart.module.mjs";
import { Box, ColorSwatch, Group, Paper, createVarsResolver, factory, getThemeColor, useMantineTheme, useProps, useResolvedStylesApi, useStyles } from "@mantine/core";
import { jsx, jsxs } from "react/jsx-runtime";
import { Legend, RadialBar, RadialBarChart, ResponsiveContainer, Tooltip as Tooltip$1 } from "recharts";
import { useState } from "react";
//#region packages/@mantine/charts/src/RadialBarChart/RadialBarChart.tsx
const defaultProps = {
	barSize: 20,
	startAngle: 90,
	endAngle: -270,
	withBackground: true,
	withTooltip: true,
	accessibilityLayer: true
};
const varsResolver = createVarsResolver((theme, { emptyBackgroundColor }) => ({ root: { "--chart-empty-background": emptyBackgroundColor ? getThemeColor(emptyBackgroundColor, theme) : void 0 } }));
const RadialBarChart$1 = factory((_props) => {
	const props = useProps("RadialBarChart", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, data, barSize, withBackground, dataKey, radialBarProps, radialBarChartProps, withLabels, withLegend, legendProps, withTooltip, tooltipProps, startAngle, endAngle, attributes, accessibilityLayer, ...others } = props;
	const [highlightedIndex, setHighlightedIndex] = useState(null);
	const getStyles = useStyles({
		name: "RadialBarChart",
		classes: RadialBarChart_module_default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const theme = useMantineTheme();
	const dataWithResolvedColor = data.map(({ color, ...item }, index) => {
		const resolvedColor = getThemeColor(color, theme);
		return {
			...item,
			__segmentIndex: index,
			fill: resolvedColor,
			fillOpacity: highlightedIndex !== null ? highlightedIndex === index ? item.opacity || 1 : .05 : item.opacity || 1
		};
	});
	const { resolvedClassNames, resolvedStyles } = useResolvedStylesApi({
		classNames,
		styles,
		props
	});
	return /* @__PURE__ */ jsx(Box, {
		...getStyles("root"),
		...others,
		children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(RadialBarChart, {
			margin: {
				top: 0,
				bottom: 0,
				left: 0,
				right: 0
			},
			innerRadius: "10%",
			outerRadius: "100%",
			cx: "50%",
			cy: "50%",
			barSize,
			startAngle,
			endAngle,
			data: dataWithResolvedColor,
			accessibilityLayer,
			...radialBarChartProps,
			children: [
				/* @__PURE__ */ jsx(RadialBar, {
					label: withLabels ? {
						position: "insideStart",
						fill: "var(--mantine-color-white)",
						fontSize: 12
					} : void 0,
					background: withBackground ? { fill: "var(--chart-empty-background)" } : void 0,
					dataKey,
					isAnimationActive: false,
					...radialBarProps
				}),
				withLegend && /* @__PURE__ */ jsx(Legend, {
					verticalAlign: "bottom",
					content: (payload) => /* @__PURE__ */ jsx(ChartLegend, {
						payload: payload.payload?.map((item) => ({
							...item,
							dataKey: item.payload?.name,
							highlightKey: item.payload?.__segmentIndex
						})),
						onHighlight: (value) => setHighlightedIndex(typeof value === "number" ? value : null),
						legendPosition: legendProps?.verticalAlign || "bottom",
						classNames: resolvedClassNames,
						styles: resolvedStyles,
						centered: true,
						attributes
					}),
					...legendProps
				}),
				withTooltip && /* @__PURE__ */ jsx(Tooltip$1, {
					animationDuration: 0,
					isAnimationActive: false,
					cursor: { stroke: "var(--chart-cursor-color)" },
					content: ({ payload }) => /* @__PURE__ */ jsxs(Paper, {
						...getStyles("tooltip"),
						children: [/* @__PURE__ */ jsxs(Group, {
							gap: "sm",
							children: [/* @__PURE__ */ jsx(ColorSwatch, {
								color: payload?.[0]?.payload.fill,
								size: 12,
								withShadow: false
							}), /* @__PURE__ */ jsx("span", { children: payload?.[0]?.payload.name })]
						}), /* @__PURE__ */ jsx("span", { children: payload?.[0]?.payload[dataKey] })]
					}),
					...tooltipProps
				})
			]
		}) })
	});
});
RadialBarChart$1.displayName = "@mantine/core/RadialBarChart";
RadialBarChart$1.classes = RadialBarChart_module_default;
RadialBarChart$1.varsResolver = varsResolver;
//#endregion
export { RadialBarChart$1 as RadialBarChart };

//# sourceMappingURL=RadialBarChart.mjs.map