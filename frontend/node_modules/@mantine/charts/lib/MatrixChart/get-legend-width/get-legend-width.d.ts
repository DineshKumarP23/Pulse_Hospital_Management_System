interface GetLegendWidthInput {
    legendLabels: [string, string];
    fontSize: number;
    colorsCount: number;
    cellSize: number;
    gap: number;
}
export declare function getLegendWidth({ legendLabels, fontSize, colorsCount, cellSize, gap, }: GetLegendWidthInput): number;
export {};
