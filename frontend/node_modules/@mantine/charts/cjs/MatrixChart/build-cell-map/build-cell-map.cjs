"use client";
//#region packages/@mantine/charts/src/MatrixChart/build-cell-map/build-cell-map.ts
function getCellKey(x, y) {
	return `${x.length}:${x}\0${y}`;
}
function buildCellMap(data) {
	const map = /* @__PURE__ */ new Map();
	data.forEach((cell) => {
		map.set(getCellKey(cell.x, cell.y), cell);
	});
	return map;
}
//#endregion
exports.buildCellMap = buildCellMap;
exports.getCellKey = getCellKey;

//# sourceMappingURL=build-cell-map.cjs.map