/**
 * Marks a portaled overlay popup as belonging to the anchor that opened it, so ancestor overlays can
 * tell "focus moved into a nested overlay of mine" from "focus left entirely" across the portal.
 */
export declare const OVERLAY_OWNER_ATTR = "data-overlay-owner";
/**
 * Property key holding the owning anchor element on a popup node. An element reference rather than an
 * id, so the link needs no id generation and cannot go stale.
 */
export declare const OVERLAY_OWNER_KEY: "__primereactOverlayOwner";
export type OverlayOwnerNode = Element & {
    [OVERLAY_OWNER_KEY]?: HTMLElement | null;
};
/**
 * Links a popup node to the anchor element that owns it.
 *
 * @param popup The portaled popup element.
 * @param anchor The anchor element the popup was opened from.
 */
export declare function setOverlayOwner(popup: OverlayOwnerNode | null | undefined, anchor: HTMLElement | null | undefined): void;
